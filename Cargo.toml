[package]
name = "petiglyph"
version = "0.1.0"
edition = "2024"
description = "Convert SVG icon sets into terminal-usable font glyphs."
license = "MIT"
repository = "https://github.com/petipoua/petiglyph"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
